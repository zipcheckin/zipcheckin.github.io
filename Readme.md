## Running the project

1. Clone the repo:
  ```
  git clone git@github.com:zipcheckin/zipcheckin.github.io.git
  ```

2. Run bundle install
  ```
  bundle install
  ```

3. Run the jekyll
  ```
  jekyll server -w
  ````